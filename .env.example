# Leave empty in development to use Vite proxy (see vite.config.ts → localhost:8080).
# For production build, set the public API origin, no trailing slash:
# VITE_BACKEND_URL=https://api.your-domain.com

VITE_BACKEND_URL=
